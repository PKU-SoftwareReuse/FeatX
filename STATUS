Current artifact status:

- Source code is available for the React frontend, Spring Boot backend, and
  Python RepoSummary module.
- The commit dataset is included at `datasets/commits/dataset.json`.
- The dataset contains 38 feature-editing commit issues across 3 repositories:
  FlappyBird (2), PlayEdu (15), and NBlog (21).
- Docker Compose deployment is included for MySQL, backend, RepoSummary Python
  dependencies, and frontend.
- The repository is licensed under the MIT License.

Locally verified on 2026-07-04:

- `docker compose config` completed successfully.
- `docker compose build backend frontend mysql` completed successfully.
- Docker images were built as `featx-backend:ase26`,
  `featx-frontend:ase26`, and `featx-mysql:ase26`.
- A clean Compose stack was started with overridden host ports:
  `BACKEND_PORT=28080 FRONTEND_PORT=23000 docker compose -p featx_ae_verify up -d --build`.
- MySQL healthcheck passed and the schema contains `code_map`, `features`,
  `graph_edge`, `modules`, and `project_info`.
- `curl -i http://localhost:28080/connect/test` returned HTTP 200.
- `curl -i http://localhost:23000/` returned the FeatX frontend HTML page.
- `curl -i http://localhost:23000/api/connect/test` returned HTTP 200 through
  the Nginx frontend proxy.
- `cd Backend && ./mvnw -DskipTests package` completed successfully.
- `npm install` completed in `Frontend/`.
- `npm run build` completed in `Frontend/`; it emitted source-map and ESLint
  warnings but produced a production build.
- `python3 -m compileall -q src` completed in `RepoSummary/`.
- The included `Backend/mvnw` wrapper is executable in this artifact snapshot.

Known limitations:

- Full LLM-backed feature extraction and code evolution require external API
  credentials and may incur provider-side cost.
- The first Docker build is large and slow because the backend image includes
  CPU PyTorch, sentence-transformers, and the Java backend. Locally observed
  image sizes were approximately 2.81 GB for `featx-backend:ase26`, 94 MB for
  `featx-frontend:ase26`, and 1.09 GB for `featx-mysql:ase26`.
- The sentence-transformer model may still be downloaded at runtime unless it is
  already cached or preloaded.

Recommended badge strategy:

- Submit for Functional: the artifact includes documented build, deployment,
  and smoke-check paths.
- Submit for Available only after archiving the exact artifact snapshot on a
  long-term repository and adding the DOI to the submission.
- Submit for Reusable if the Docker Compose workflow and LLM credential
  requirements are acceptable to the Artifact Evaluation committee.
